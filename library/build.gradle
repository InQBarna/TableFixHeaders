apply plugin: 'com.android.library'

android {
    compileSdk 32
    buildToolsVersion '32.0.0'

    defaultConfig {
        minSdkVersion 5
        targetSdk 32

    }
}

dependencies {
    api fileTree(dir: 'libs', include: ['*.jar'])
}
